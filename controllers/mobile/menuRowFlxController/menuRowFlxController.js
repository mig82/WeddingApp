define({
    /*This controller was created when I created the template for the segment. I belive as I copy the
    menu around the controller will be duplicated as well. I don't want to write the same
    controller several times so I'm not using this.*/
});